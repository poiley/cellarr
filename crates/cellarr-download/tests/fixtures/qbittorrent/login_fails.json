{
  "scenario": "qBittorrent bad credentials: login returns 200 with body 'Fails.' and no cookie -> Auth error",
  "exchanges": [
    {
      "expect": { "method": "POST", "url_contains": "/api/v2/auth/login" },
      "response": { "status": 200, "headers": {}, "body": "Fails." }
    }
  ]
}
